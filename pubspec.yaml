name: portfolio_app
description: A Flutter portfolio web app.
publish_to: 'none' # Private package

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"  # Compatible with stable Flutter/Dart

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Add your assets here if you have any
  assets:
    - assets/images/cover.jpg
    - assets/icons/

  # Add custom fonts if needed
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  
