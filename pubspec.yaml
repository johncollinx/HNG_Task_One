name: portfolio_app
description: A Flutter portfolio web app.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # Added Google Fonts for enhanced web typography
  google_fonts: ^6.1.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # 💡 Important Note for Web Deployment: 
  # If deployed to a subpath (e.g., GitHub Pages URL), you must build 
  # with the base-href argument: flutter build web --base-href /repo-name/

  assets:
    - assets/images/
    - assets/icons/

  # Uncomment if you add custom fonts later
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
